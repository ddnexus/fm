#!/usr/bin/env zsh

__fm-copy-paths() {
    local -P res
    if [[ -z $FM_CMD_XCLIP ]] { __fm-notify 'No FM_CMD_XPLIP command found'; return 0 }
    print -n -- ${(@qqq)items} | $=FM_CMD_XCLIP
    res=$?
    if [[ $res -gt 0 ]]; then
        fm-notify "0 Paths copied to x-clipboard [ERROR $res]"
    else
        __fm-notify "$#items Paths copied to x-clipboard"
    fi
}
